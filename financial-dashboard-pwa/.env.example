# Copy to `.env` (git-ignored) before running `npm run dev` or `npm run build`.
# Both values are safe to expose publicly: RLS restricts every row to auth.uid().
# Leave them blank to run the app fully offline/local-only.
# SECURITY: the values below are placeholders. Paste your own Supabase project
# URL + anon key here, and never commit the real `.env` file.
VITE_SUPABASE_URL=https://example.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_example_placeholder_rotate_me

