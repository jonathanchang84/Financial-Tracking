# Copy to `.env` (git-ignored) before running `npm run dev` or `npm run build`.
# Leave blank when the PWA and Worker share an origin. The browser only needs
# the public API origin; never put a Worker secret here.
#
# The app sends no email, so there is no mail API key of any kind.
VITE_API_URL=

